import math
import os
import random
import pickle
import torch
from torch.utils.data import DataLoader, Dataset


def read_data(data_dir=os.path.join('./', 'data'), dataset='amazon'):
    with open(data_dir, 'r') as f:
        pass



